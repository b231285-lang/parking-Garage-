import 'dotenv/config';import express from 'express';import cors from 'cors';import mongoose from 'mongoose';import auth from './routes/auth.js';import parking from './routes/parking.js';
const app=express();app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(express.json());app.get('/api/health',(q,s)=>s.json({ok:true}));app.use('/api/auth',auth);app.use('/api/parking',parking);app.use((e,q,s,n)=>{console.error(e);s.status(e.status||500).json({message:e.message||'Server error'})});const port=process.env.PORT||5000;mongoose.connect(process.env.MONGO_URI||'mongodb://127.0.0.1:27017/parking_garage').then(()=>app.listen(port,()=>console.log(`API running on ${port}`))).catch(e=>{console.error(e);process.exit(1)});
